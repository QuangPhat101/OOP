## 1. Cách vẽ hình 
- <virtual> salary(): int = 0
- <override> salary(): int -> override phải viết trước hàm 
Hành vi giống nhau thì không cần virtual

2. Đề thi 
Phần đầu: được sửa class cha
Phần cuối: Không được sửa class cha, phải tự tìm giải pháp 

3. virtual ~Employee() -> ép đa hình phân giải đúng 
